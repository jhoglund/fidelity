<%= fidelity.widget(:tabs, :outline => :contained, :orientation => :vertical, :style => :nice) do |t| %>
	<%= t.tab('First tab') %>
	<%= t.tab('Second tab', :state => :active) %>
	<%= t.tab('Third tab') %>
<% end %>
